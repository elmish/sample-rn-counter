{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "fable-watch",
            "type": "shell",
            "command": "dotnet",
            "args": [ "fable", "watch", "src/app.fsproj", "-o", "out", "--define", "DEBUG" ],
            "isBackground": true,
            "problemMatcher": "$msCompile"
        }
    ]
}